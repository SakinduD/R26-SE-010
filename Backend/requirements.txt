# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database / ORM
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10

# Auth / validation
python-jose[cryptography]==3.3.0
email-validator==2.2.0

# Settings / env
pydantic==2.9.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# HTTP client
httpx==0.27.2

# Supabase
supabase==2.10.0

# LLM / AI SDKs
# Legacy SDK — used by llm_service.py (MCA chat) and mca_live_scorer.py
google-generativeai==0.8.3
# New SDK — used by core/llm_client.py (APM + RPE Gemini calls)
google-genai>=0.3.0
# Groq SDK — used by rpe_llm_service.py (NPC dialogue fallback + end detection)
groq

# ML / Signal processing
numpy==1.26.4
librosa==0.10.2.post1
soundfile==0.12.1
scikit-learn==1.5.2
joblib==1.4.2
# PyAV — used by nudge_engine.AudioFeatureExtractor to decode WebM/Opus audio
av>=12.0.0
# Wav2Vec2 SER model (loaded lazily when model_config.json enables wav2vec2)
transformers==4.40.0
torch==2.2.2
datasets==2.19.0

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
