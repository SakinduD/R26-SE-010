# Frontend environment example for Vite
# Copy to `.env.local` or `.env` in the `frontend/` folder and update values.

# Base URL for backend API (must start with http(s)://)
VITE_API_URL=http://localhost:8000